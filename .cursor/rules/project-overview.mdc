---
description: picoo 项目概览与公开 API 冻结规则
globs:
alwaysApply: true
---

# picoo — WASM 图片处理

## 技术栈

- Rust crate：`picoo-core`（`crates/picoo-core`）
- npm 包：`picoo`（`packages/core`）
- 经 wasm-pack 产出 WASM，外层为 TypeScript Worker 封装

## 冻结的公开 API

- 唯一入口：`createImageProcessor()`
- 方法：`info`、`infoBatch`、`process`、`processBatch`、`dispose`
- IO 辅助：`picoo/io`（`toBytes`、`toBlob`、小程序相关 helpers）

不要新增 `initWasm`、`compressToSize`、链式 builder 等顶层导出。

## AI 协作约定

1. 最小 diff — 不做顺手重构
2. API 变更需先更新计划 / issue
3. 完成前运行 `cargo test` 与 `npm test`
4. 依赖变更后检查 WASM 体积
5. 用户可见文档默认使用中文
