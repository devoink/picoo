---
description: picoo-core 的 Rust / WASM 约束
globs: crates/**/*.rs
---

# Rust / WASM 规则

- wasm-bindgen 导出保持精简：`get_image_info`、`process_image`
- 选项以 JSON 字符串传入，不要逐字段 bindgen
- 用户错误一律通过 JsValue 返回 `{ code, message }`
- 避免坏输入导致 panic — 返回 `PicooError`
- `wasm-pack` profile：`wasm-opt = false`（在 `build.sh` 中带 SIMD 标志跑 wasm-opt）
- 改完后：`cargo test` 与 `cargo clippy -- -D warnings`

## WASM 上的 WebP

使用 `zenwebp`：默认有损 VP8（`quality` 1–100），`lossless: true` 为 VP8L。`maxSizeKB` 对有损 WebP 做质量二分；无损 WebP 仅在开启 `autoResize` 时通过缩小尺寸逼近体积。
