{
  "name": "picoo-browser-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pretest:e2e": "test -f ../../packages/core/pkg-web/picoo_core_bg.wasm || (echo 'Missing WASM: run ../../scripts/build.sh first' >&2 && exit 1)",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium --with-deps"
  },
  "dependencies": {
    "picoo": "file:../../packages/core"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vite-plugin-top-level-await": "^1.4.4",
    "vite-plugin-wasm": "^3.4.1"
  }
}
